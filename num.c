#include<stdio.h>
#include<math.h>
int main()
{
 int i,n;
 printf("enter the number n:");
 scanf("%d",&n);
 if (n==0)
	 printf("the number %d is 0;",n);
 else
	if(n>0)
	  
	   printf("the number %d  is positive integer",n);
	 else 
	   printf("the number %d is negative  integer",n);
return 0;
}